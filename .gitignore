node_modules/
.env
.env.local

# Never commit private keys or cert bundles.
certs/*
!certs/.gitkeep
*.p12
*.pem
*.key

.DS_Store

# The server serves the .txt route from the extensionless file. Keeping a
# second copy on disk only creates a chance for the two to drift apart.
well-known/*.txt
